/**
 * Vitest global setup for takos-cli tests.
 *
 * This file is loaded before every test suite via `setupFiles` in
 * vitest.config.ts. Add shared mocks, global stubs, or environment
 * variable overrides here.
 */
